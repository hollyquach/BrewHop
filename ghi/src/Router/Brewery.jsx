export default function Brewery() {
    // Add an if/else if no card is searched.
    // "You need to select a brewery to view the details"
    return (
        <div className="App">
            <header className="App-header">
                <p>Brewery Details</p>
            </header>
        </div>
    )
}